# === AI Providers (add only the ones you want) ===
GROQ_API_KEY=                    # Free tier — get from console.groq.com
GEMINI_API_KEY=                  # Free tier — get from aistudio.google.com

# === Telegram Bot ===
TELEGRAM_BOT_TOKEN=              # Get from @BotFather
TELEGRAM_CHAT_ID=                # Your personal chat ID (get from @userinfobot)

# === Fitbit (Food Logger) ===
FITBIT_CLIENT_ID=                # Get from dev.fitbit.com/apps
FITBIT_CLIENT_SECRET=            # Get from dev.fitbit.com/apps
FITBIT_REDIRECT_URI=http://localhost:3000/api/fitbit/callback

# === Database ===
DATABASE_URL=postgresql://agent:agent@localhost:5432/cherry_agent

# === Redis ===
REDIS_URL=redis://localhost:6379

# === Agent Config ===
AGENT_MAX_DAILY_SPEND_USD=0.50   # Hard daily cap
AGENT_MAX_MONTHLY_SPEND_USD=5.00 # Hard monthly cap
AGENT_DEFAULT_MODEL_TIER=1       # Default tier for new tasks
AGENT_DRY_RUN=false              # Set true to log API calls without sending

# === User Settings ===
USER_TIMEZONE=America/Toronto       # IANA timezone for date-sensitive logging

# === Surpride (Inspiration Board) ===
SURPRIDE_WEBHOOK_URL=https://surpride.samantafluture.com/api/webhooks/inspiration
SURPRIDE_WEBHOOK_TOKEN=

# === YouTube / Brain ===
BRAIN_DIR=                       # Path to brain context files (e.g. ~/Development/brain or /app/brain in Docker)

# === Voice Coding Pipeline ===
VOICE_REPO_BASE_PATH=/home/sam/apps  # Base directory containing git repos
GITHUB_TOKEN=                        # GitHub personal access token for PR operations

# === Environment ===
NODE_ENV=development
PORT=3000
LOG_LEVEL=info
